# Generated database types

Start the local Supabase stack and regenerate types after every schema migration:

```bash
npm run db:start
npm run db:reset
npm run db:types
```

`database.generated.ts` is generated by Supabase CLI and must not be edited manually.
